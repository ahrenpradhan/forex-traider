import React from 'react';
import $ from 'jquery';

export default class Events extends React.Component {
    render() {
        return (
            <div>
                Dashboard
            </div>
        )
    }
}